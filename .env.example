# Development Environment Variables
# This file is for local development only and should not be committed to version control

# Site Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME=Enterprise EC Demo

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Database Configuration (for future implementation)
DATABASE_URL=sqlite:./dev.db

# JWT Authentication
JWT_SECRET=your-jwt-secret-key-change-in-production-min-32-chars
JWT_REFRESH_SECRET=your-jwt-refresh-secret-key-change-in-production-min-32-chars
JWT_ISSUER=enterprise-ec-demo
JWT_AUDIENCE=enterprise-ec-demo

# Authentication (for future implementation)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-development-secret-key-change-in-production

# Feature Flags
NEXT_PUBLIC_ENABLE_ANALYTICS=false
NEXT_PUBLIC_ENABLE_CHAT=false
NEXT_PUBLIC_ENABLE_RECOMMENDATIONS=false

# Payment Configuration (for future implementation)
STRIPE_PUBLIC_KEY=pk_test_your_stripe_public_key
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# Email Configuration (for future implementation)
EMAIL_FROM=noreply@localhost
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Storage Configuration (for future implementation)
UPLOAD_DIR=./public/uploads
MAX_FILE_SIZE=5242880

# Monitoring (for future implementation)
ENABLE_LOGGING=true
LOG_LEVEL=info

# Demo Configuration
DEMO_MODE=true
MOCK_PAYMENTS=true
MOCK_EMAILS=true